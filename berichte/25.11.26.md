# Arbeitsbericht vom 26.11.2025

- Thema: c input\output
- Name: Maximilian Joszt
- Klasse: 2 AHITS Gruppe 1
- Fach: ITS1 Übungen


---

## g++ -o summiere summieren.cpp

Damit wird die Datei kompiliert (g++ ist der c Kompiler) und erzeugt dann ein ausführbares Programm das `o-` sorgt für den Namen.

---

## ./(Name der Datei)

Damit kann man Programe in einer shell aufrufen ohne auf run oder den grünen pfeil zu klicken.

z.b.:

```shell
./prog


```
damit wird das Programm `prog` gestartet dies wird dann so ausgegeben:

```shell
Hallo Welt
```



## Übung 2.5 (Cin/out)


 Aufgabe: Erstelle eine Datei sumieren.cpp und öffne diese im Replit Editor.

 ```shell
 touch sumieren.cpp
 ```

 Aufgabe: Schreibe ein C Programm das zuerst eine Anzahl (int n) einliest.

Dann werden n weitere ganze Zahlen eingelesen und die Summe dieser Zahlen ermittelt.

Am Ende gibt das Programm die Summe aus (dies soll die einzige Ausgabe des Programms sein).
Beispiel-Programmablauf (n=3):

```c
#include <stdio.h>

int main()
{
 int n = 0;
 int anzahl = 0;
  int y = 0;

  scanf("%d", &anzahl);

  for(int i=0;i < anzahl;i++)
    {
      scanf("%d",&y);
      n = n + y;


    }
  printf("%d",n);
  return 0;


}
```

---

Aufgabe: Compiliere das Programm mit folgender Kommandozeile: g++ -o summiere summieren.cpp

```shell
g++ -o summiere summieren.cpp

```

---

Aufgabe: Behebe evtl. Compiler-Fehler und starte dann das Programm mit: `./summiere`

```shell
./summiere

```

```shell
5
1
2
3
4
5
15
```




Aufgabe: Rufe nun das Programm auf und leite die Textdatei per Dateiumleitung ins stdin des Programms summiere um. Schreibe weiters den Output des Programms in eine Textdatei.

```shell
./summiere <eingabe.txt >ausgabe.txt

```










 

 



